[user]
    name = Zak Johnson
    email = zakj@nox.cx

[alias]
    box = !open -a Gitbox .
    ci = commit
    co = checkout
    d = diff
    dc = diff --cached
    graph = log --graph --oneline
    out = log origin/master..HEAD
    st = status --branch --short
    unstage = reset HEAD --

[core]
    pager = cat
