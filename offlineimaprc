[general]
accounts = nox.cx
ui = basic

[Account nox.cx]
localrepository = Local
remoterepository = Remote
#autorefresh = 1
quick = 5

[Repository Local]
type = Maildir
localfolders = ~/Mail

[Repository Remote]
cert_fingerprint = 6d1b5b5ee0180ab493b71d3b94534b5ab937d042
type = Gmail
remoteuser = zakj@nox.cx
remotepassfile = ~/.mutt/pw
realdelete = no
# Always sync INBOX first.
foldersort = lambda a, b: -1 if a == 'INBOX' else 1 if b == 'INBOX' else cmp(a, b)
# Ignore "All Mail".
folderfilter = lambda folder: folder not in ['[Gmail]/All Mail', '[Gmail]/Spam']
